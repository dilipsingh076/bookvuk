# BookNest Backend (Placeholder)

This repository currently contains only the frontend scaffold requested.

When you are ready, you can add an fastApi backend here and replace the placeholder API functions in `frontend/src/api/index.js`.

