/** Tuning local to the inventory screen. */

/** At or below this many copies a title counts as running out. Ten is roughly a
 *  week of movement on a mid-list title here — early enough to reorder. */
export const LOW_STOCK_THRESHOLD = 10;

/** The default restock quantity offered in the dialog. */
export const DEFAULT_RESTOCK_QTY = 10;
