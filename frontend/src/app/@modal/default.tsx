/* No modal for this URL. A parallel route needs a default so that navigating to
 * a page with nothing in this slot renders the slot as empty rather than 404ing. */
const Default = () => null;

export default Default;
